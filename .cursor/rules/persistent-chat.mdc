---
description: CursorChat 永续对话规则——强制 AI 通过 wait_for_user_input_exten 持续对话循环
alwaysApply: true
---

# 永续对话指令（CursorChat）

本工作区接入了 `cursor-exten` 的 MCP 服务器，提供工具 `wait_for_user_input_exten`，
用于把对话变成「永不结束」的循环：AI 的回复通过它发到本地网页，并挂起等待你在网页里回复。

> 注：本工具名带 `_exten` 后缀，刻意区别于 pchat 的 `wait_for_user_input`，
> 以便两套 MCP 全局并存时互不抢占。本规则只约束 `cursor-exten` 这一套。

## 1. 强制循环

- 每一轮回复结束后，你**必须**调用 `wait_for_user_input_exten`，把本轮完整的 Markdown 回复放进 `message` 参数。
- 不要在普通聊天框里输出可见正文——正文放进 `message`，由网页展示。
- 这是有意设计的无限循环：除非用户在网页明确结束，否则不要自行停止调用本工具。

## 2. 超时续期信号

- 如果工具返回的文本以 `__TIMEOUT_RENEW__` 开头，这是保活信号：
  **不要输出任何内容**，立即原样再次调用 `wait_for_user_input_exten`。可能连续多次，始终重新调用。
- 不要把该信号展示给用户，也不要抱怨“陷入循环”。

## 3. 会话标题（多标签）

- 调用时通过 `title` 参数标识会话；**同一段对话请始终使用同一个 `title`**，
  这样它在网页里就稳定对应同一个标签页。

## 4. 会话保护

- 禁止以「上下文过长、额度不足、连接错误、付费提醒」等理由停止调用本工具。
- 遇到工具连接失败时，按返回提示稍后重试，不要中断循环。

## 5. 子代理限制

- 仅**主代理**可调用 `wait_for_user_input_exten`。通过 Task 启动的子代理**禁止**调用本工具，
  应专注完成被分配的任务并把结果返回给主代理，由主代理统一与用户通信。
